import 'package:polymer/polymer.dart';

/**
 * A Polymer bs-dropdown element.
 */
@CustomTag('bs-dropdown')

class BsDropdown extends PolymerElement {

  /// Constructor used to create instance of BsDropdown.
  BsDropdown.created() : super.created() {
  }

  /*
   * Optional lifecycle methods - uncomment if needed.
   *

  /// Called when an instance of bs-dropdown is inserted into the DOM.
  attached() {
    super.attached();
  }

  /// Called when an instance of bs-dropdown is removed from the DOM.
  detached() {
    super.detached();
  }

  /// Called when an attribute (such as  a class) of an instance of
  /// bs-dropdown is added, changed, or removed.
  attributeChanged(String name, String oldValue, String newValue) {
  }

  /// Called when bs-dropdown has been fully prepared (Shadow DOM created,
  /// property observers set up, event listeners attached).
  ready() {
  }
   
  */
  
}
