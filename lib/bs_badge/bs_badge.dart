import 'package:polymer/polymer.dart';

/**
 * A Polymer bs-badge element.
 */
@CustomTag('bs-badge')

class BsBadge extends PolymerElement {

  /// Constructor used to create instance of BsBadge.
  BsBadge.created() : super.created() {
  }

  /*
   * Optional lifecycle methods - uncomment if needed.
   *

  /// Called when an instance of bs-badge is inserted into the DOM.
  attached() {
    super.attached();
  }

  /// Called when an instance of bs-badge is removed from the DOM.
  detached() {
    super.detached();
  }

  /// Called when an attribute (such as  a class) of an instance of
  /// bs-badge is added, changed, or removed.
  attributeChanged(String name, String oldValue, String newValue) {
  }

  /// Called when bs-badge has been fully prepared (Shadow DOM created,
  /// property observers set up, event listeners attached).
  ready() {
  }
   
  */
  
}
